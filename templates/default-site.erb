server {
  listen   80;
  server_name  <%= node['hostname'] %>;

  access_log  <%= node['nginx']['log_dir'] %>/default-site.access.log;

  location / {
    root   /var/www;
    index  index.html index.htm;
  }
}
